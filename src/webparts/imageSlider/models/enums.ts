export enum imageSize {
  Small = 0,
  Medium = 1,
  Large = 2,
  XLarge = 3
}

export enum displayView {
  AllImages = 0,
  EnabledOnly = 1,
  PublicDates = 2
}

export enum orderBy {
  Modified = 0,
  Created = 1,
  DisplayOrder = 2
}

export enum orderByDirection {
  Ascending = 0,
  Descending = 1,
}